package com.ausgetrunken.ui.theme

import androidx.compose.ui.graphics.Color

// Wine-inspired primary colors
val WineRed = Color(0xFF722F37)           // Burgundy wine red
val WineRedLight = Color(0xFF9C4146)      // Lighter burgundy
val WineAccent = Color(0xFFB8860B)        // Dark goldenrod
val WineGold = Color(0xFFD4AF37)          // Rich gold

// Dark theme colors
val DarkBackground = Color(0xFF0D0D0D)    // Very dark background
val DarkSurface = Color(0xFF1A1A1A)       // Dark card surface
val DarkSurfaceVariant = Color(0xFF2A2A2A) // Slightly lighter cards
val DarkOnSurface = Color(0xFFE6E6E6)     // Light text on dark
val DarkOnSurfaceVariant = Color(0xFFB8B8B8) // Muted text
val DarkOutline = Color(0xFF3A3A3A)       // Subtle borders

// Light theme colors  
val LightBackground = Color(0xFFFFFBFF)   // Warm white
val LightSurface = Color(0xFFFFFFFF)      // Pure white cards
val LightSurfaceVariant = Color(0xFFF5F2F0) // Warm off-white
val LightOnSurface = Color(0xFF1C1B1F)    // Dark text on light
val LightOnSurfaceVariant = Color(0xFF625B71) // Muted text
val LightOutline = Color(0xFFE4E1E6)      // Light borders

// Wine bottle colors for placeholders
val RedWineBottle = Color(0xFF2D1B2E)     // Dark burgundy
val WhiteWineBottle = Color(0xFFF5F5DC)   // Beige/cream
val RoseWineBottle = Color(0xFFFFB6C1)    // Light pink
val SparklingWineBottle = Color(0xFFF0F8FF) // Alice blue

// Error and success colors
val ErrorRed = Color(0xFFBA1A1A)
val SuccessGreen = Color(0xFF2E7D32)